# Netlify configuration for BarkBase Demo
# SPA redirect - all routes serve index.html

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_DEMO_MODE = "true"
