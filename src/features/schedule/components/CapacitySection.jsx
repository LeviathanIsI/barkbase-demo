import CapacityOverviewSection from "@/features/calendar/components/CapacityOverviewSection";

/**
 * Capacity section for schedule page
 */
const CapacitySection = ({ currentDate }) => {
  return <CapacityOverviewSection currentDate={currentDate} />;
};

export default CapacitySection;

